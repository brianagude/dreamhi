<section class="products">
  <div class="container">
    <div class="row">
      <% @products.each do |product| %>
        <% if product.is_sold_out==true %>
          <div class="sold-out col-lg-4 col-xs-12 product">
            <%= image_tag product.image_1.medium.url, class: "image"%>
            <h5><%= product.title%></h5>
            <p><%= number_to_currency product.price, precision: 0%></p>
            <div class="overlay">
              <div class="text"><h2>SOLD OUT</h2></div>
            </div>
          </div>
        <% else %>
          <div class="col-lg-4 col-sm-12 product">
            <%= link_to product_path(product) do %>
              <%= image_tag product.image_1.medium.url %>
              <h5><%= product.title%></h5>
              <p><%= number_to_currency product.price, precision: 0%></p>
            <% end %>
          </div>
        <% end %>
      <% end %>
    </div>
  </div>
</section>
